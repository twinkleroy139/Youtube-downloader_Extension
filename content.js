


// now its not important its just a placeholder
if (chrome.runtime && chrome.runtime.onMessage) {
  chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {
    // Placeholder for future logic
  });
}
